# WhatsApp Bridge Configuration
# Set the host and port where the WhatsApp bridge is running
# For local setup: localhost
# For cloud setup: your-server.com or IP address

BRIDGE_HOST=localhost
BRIDGE_PORT=8080

# Database path (usually auto-detected)
# DOCKER_DB_PATH=/data/store/messages.db
# LOCAL_DB_PATH=../whatsapp-bridge/store/messages.db