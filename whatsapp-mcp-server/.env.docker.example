# Docker configuration example
# Copy this to .env when running MCP server inside Docker container
# (Not recommended for normal usage)

# WhatsApp Bridge connection settings
BRIDGE_HOST=whatsapp-bridge-host
BRIDGE_PORT=8080

# Database configuration - set to true when running in Docker container
USE_DOCKER_DB=true

# WhatsApp API base URL (auto-generated from HOST and PORT)
# WHATSAPP_API_BASE_URL=http://whatsapp-bridge:8080/api